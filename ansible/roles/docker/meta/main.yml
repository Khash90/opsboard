---
# SPDX-License-Identifier: MIT-0

galaxy_info:
  role_name: docker
  namespace: opsboard
  author: Khash90
  description: Install Docker build and local testing tools on an OpsBoard build host.
  license: MIT-0
  min_ansible_version: "2.16"

  platforms:
    - name: Ubuntu
      versions:
        - noble

  galaxy_tags:
    - docker
    - containers
    - buildx
    - compose
    - ubuntu

dependencies: []
